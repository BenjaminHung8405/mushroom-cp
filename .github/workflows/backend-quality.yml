name: Backend quality

on:
  push:
  pull_request:

jobs:
  backend-quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mushroom-backend
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: mushroom-backend/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - name: Verify Compose backend authentication configuration contract
        run: node ../scripts/verify-backend-auth-config.mjs
      - run: pnpm run typecheck
      - name: Lint every changed TypeScript file since the merge base
        env:
          LINT_BASE_REF: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
        run: pnpm run lint
      - name: Verify reviewed legacy lint-debt baseline
        run: pnpm run lint:baseline
      - run: pnpm test -- --runInBand
